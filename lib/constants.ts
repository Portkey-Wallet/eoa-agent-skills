export const CHAIN_IDS = {
  AELF: 'AELF',
  TDVV: 'tDVV',
  TDVW: 'tDVW',
} as const;

export const DEFAULT_TIMEOUT_MS = 30_000;
export const RPC_HTTP_TIMEOUT_MS = DEFAULT_TIMEOUT_MS;

export const NFT_IMAGE_SIZE = {
  width: 300,
  height: 300,
} as const;

export const ACTIVITY_IMAGE_SIZE = {
  width: 120,
  height: 120,
} as const;

